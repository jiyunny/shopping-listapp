# Shopping List App

순수 HTML/CSS/JavaScript로 만든 쇼핑 리스트 웹 앱입니다. 외부 라이브러리 없이 단일 `index.html` 파일로 동작하며, 브라우저의 `localStorage`를 이용해 항목을 저장합니다.

## 기능

- 아이템 추가 (입력 후 Enter 또는 추가 버튼)
- 아이템 완료 체크/해제
- 아이템 삭제
- 체크된 항목 일괄 삭제
- 라이트/다크 모드 자동 대응

## 사용 방법

`index.html` 파일을 브라우저에서 열면 바로 사용할 수 있습니다.
